lexik_jwt_authentication:
    secret_key: '%env(base64:JWT_PRIVATE_KEY_BASE64)%'
    public_key: '%env(base64:JWT_PUBLIC_KEY_BASE64)%'
    pass_phrase: '%env(JWT_PASSPHRASE)%'
    token_ttl: 3600 # 1 hour
