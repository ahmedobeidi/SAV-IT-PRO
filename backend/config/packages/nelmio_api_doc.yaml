nelmio_api_doc:
    documentation:
        info:
            title: API SAV IT Pro
            description: Documentation de l’API SAV IT Pro
            version: 1.0.0
        components:
            securitySchemes:
                bearerAuth:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
    areas:
        default:
            path_patterns:
                - ^/api(?!/doc$)